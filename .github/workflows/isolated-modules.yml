name: isolated-modules

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  bun:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mode: [isolated, hoisted]
    steps:
      - name: 🏗 Setup repository
        uses: actions/checkout@v4

      - name: 🏗 Setup monorepo
        uses: ./.github/actions/setup-monorepo
        with:
          install-modules: 'no'

      - name: 🏗 Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: ⚙️ Configure Bun
        run: |
          git rm .npmrc pnpm-lock.yaml pnpm-workspace.yaml
          echo $(jq '.workspaces = ["apps/*","packages/*"]' package.json) > package.json
          cat package.json | jq

      - name: ⚙️ Configure isolation
        if: ${{ matrix.mode == 'isolated' }}
        run: |
          touch bunfig.toml
          echo "[install]" >> bunfig.toml
          echo "linker = \"isolated\"" >> bunfig.toml

      - name: 📦 Install dependencies
        run: bun install

      - name: 👷 Build packages
        run: bun run build

      - name: 🧪 Test packages
        run: bun run test

  npm:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mode: [isolated, hoisted]
    steps:
      - name: 🏗 Setup repository
        uses: actions/checkout@v4

      - name: 🏗 Setup monorepo
        uses: ./.github/actions/setup-monorepo
        with:
          install-modules: 'no'

      - name: ⚙️ Configure npm
        run: |
          git rm .npmrc pnpm-lock.yaml pnpm-workspace.yaml
          echo $(jq '.workspaces = ["apps/*","packages/*"]' package.json) > package.json
          cat package.json | jq

      - name: 📦 Install dependencies
        if: ${{ matrix.mode == 'isolated' }}
        run: npm install --legacy-peer-deps --install-strategy=linked

      - name: 📦 Install dependencies
        if: ${{ matrix.mode == 'hoisted' }}
        run: npm install --legacy-peer-deps

      - name: 👷 Build packages
        run: npm run build

      - name: 🧪 Test packages
        run: npm run test


  pnpm:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mode: [isolated, hoisted]
    steps:
      - name: 🏗 Setup repository
        uses: actions/checkout@v4

      - name: 🏗 Setup monorepo
        uses: ./.github/actions/setup-monorepo
        with:
          install-modules: 'no'

      - name: ⚙️ Configure isolation
        if: ${{ matrix.mode == 'isolated' }}
        run: git rm .npmrc

      - name: 📦 Install dependencies
        run: pnpm install

      - name: 👷 Build packages
        run: pnpm run -w build

      - name: 🧪 Test packages
        run: pnpm run -w test
