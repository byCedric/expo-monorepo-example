{
  "private": true,
  "name": "@acme/monorepo",
  "scripts": {
    "build": "pnpm run -r --if-present build",
    "test": "pnpm run -r --if-present test"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": ["@babel/*"]
    }
  }
}
