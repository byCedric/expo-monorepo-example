{
  "private": true,
  "name": "@acme/monorepo",
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "dev": "pnpm run -r --if-present --parallel dev"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@babel/*"
      ]
    }
  },
  "devDependencies": {
    "turbo": "^1.4.6"
  }
}
